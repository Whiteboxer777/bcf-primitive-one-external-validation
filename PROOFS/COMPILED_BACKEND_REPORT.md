# COMPILED_BACKEND_REPORT

Current status:
- compiler emits `COMPILED_BACKEND.json`
- compiler emits `COMPILED_BACKEND.schema.json`
- verifier recomputes and checks compiled backend artifact
- compiled backend gate runs on kernel profiles and requests
- current status is executable-witness strong for the compiled-backend preparation family only
