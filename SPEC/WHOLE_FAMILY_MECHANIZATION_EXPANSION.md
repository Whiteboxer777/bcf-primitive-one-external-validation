# WHOLE_FAMILY_MECHANIZATION_EXPANSION

## Status

This document defines the executable-witness expansion layer that bridges the mechanized semantic kernel, Admission Normal Form, compiled backend preparation, and witness-certificate preparation.

It fixes a scoped whole-family mechanization expansion objective:

- semantic core theorem witnesses remain mechanized at finite-state kernel level
- ANF remains verdict-preserving relative to the reference evaluator on the canonical kernel corpus
- compiled backend remains verdict-preserving relative to ANF and the reference evaluator on the canonical kernel corpus
- verification, permit, refusal, replay, and no-bypass outputs remain witness-certificate-carrying and schema-closed on the current canonical kernel corpus
- fail-closed and no-bypass behavior remain preserved across these execution layers

This document does **not** claim whole-family theorem-prover-grade discharge.
It defines an executable-witness expansion line across the highest-value semantic families already present in the current primitive release.

## Whole-family mechanization expansion objective

The objective of this layer is to establish one explicit cross-layer preservation spine:

1. reference semantic kernel
2. Admission Normal Form
3. compiled backend preparation
4. witness-certificate-carrying verification / permit / refusal / replay outputs
5. fail-closed and no-bypass preservation over the above

The output of this layer is an executable gate and report proving that these surfaces cohere on the current kernel-scoped canonical corpus.

## Controlled scope

Inside scope:
- verdict-parity preservation across reference / ANF / compiled backend on canonical requests
- witness-certificate structural soundness for verification / permit / refusal / replay outputs
- no-bypass refusal witness soundness
- fail-closed preservation for canonical deny and broken-bundle paths
- gate-backed report generation

Outside scope:
- whole-family theorem-prover closure
- complete object-model mechanization
- complete canonicalization-family mechanization
- scalable-backend correctness beyond the current kernel corpus
- formal proof that every future backend preserves these semantics
